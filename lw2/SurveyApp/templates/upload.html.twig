<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <title>Текущие значения</title>
    </head>
    <body>
        <p>{{ alert }}</p>
        <p>First Name: {{ firstName }}</p>
        <p>Last Name: {{ lastName }}</p>
        <p>Age: {{ age }}</p>
        <p>Email: {{ email }}</p>
    </body>
</html>